name: Build and Deploy to Heroku

on:
  push:
    branches: [ develop ]

jobs:
  build-and-deploy:

    runs-on: ubuntu-latest
    env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}    

    strategy:
      matrix:
        node-version: [16.x]

    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2.3.4
      
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install and Build and test 🔧
      run: |
        npm ci
        npm run build --if-present

    - name: deploy heroku
      uses: CDNievas/heroku-action@v1.0 # This is the action    
      # uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
      with:
        heroku_api_key: ${{secrets.HEROKU_API_KEY}}
        heroku_app_name: jcc2303 #Must be unique in Heroku
        heroku_email: cabrera.javier@gmail.com
        branch: develop # should a var from github
        # appdir: dist
        # procfile: "web: npm start"
